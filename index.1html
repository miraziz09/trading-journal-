<!DOCTYPE html>
<html lang="uz">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Trading Journal</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=IBM+Plex+Mono:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
:root{
  --bg:#0a0d12;
  --panel:#10141b;
  --panel-2:#151a23;
  --border:#222831;
  --border-soft:#1b2028;
  --text:#e8ecf1;
  --text-dim:#8a93a3;
  --text-faint:#5b6577;
  --accent:#caa45e;
  --accent-soft:rgba(202,164,94,0.12);
  --win:#3ddc97;
  --win-soft:rgba(61,220,151,0.12);
  --loss:#ff5d6c;
  --loss-soft:rgba(255,93,108,0.12);
  --be:#7c8aa5;
  --be-soft:rgba(124,138,165,0.12);
  --radius:10px;
  --mono:'IBM Plex Mono', monospace;
  --sans:'Inter', sans-serif;
}
*{box-sizing:border-box;}
html,body{margin:0;padding:0;}
body{
  background:var(--bg);
  color:var(--text);
  font-family:var(--sans);
  -webkit-font-smoothing:antialiased;
  min-height:100vh;
}
::-webkit-scrollbar{width:10px;height:10px;}
::-webkit-scrollbar-track{background:transparent;}
::-webkit-scrollbar-thumb{background:#252b35;border-radius:6px;}
::-webkit-scrollbar-thumb:hover{background:#313844;}

a{color:inherit;}

/* ===== Layout ===== */
.app{max-width:1400px;margin:0 auto;padding:20px 24px 60px;}
@media (max-width:768px){.app{padding:14px 12px 40px;}}

header.topbar{
  display:flex;align-items:center;justify-content:space-between;
  padding:14px 4px 22px;border-bottom:1px solid var(--border-soft);margin-bottom:22px;
  flex-wrap:wrap;gap:12px;
}
.brand{display:flex;align-items:center;gap:12px;}
.brand-mark{
  width:36px;height:36px;border-radius:8px;
  background:linear-gradient(145deg,var(--accent),#8a6f3e);
  display:flex;align-items:center;justify-content:center;
  font-family:var(--mono);font-weight:700;color:#0a0d12;font-size:15px;
  flex-shrink:0;
}
.brand-text h1{font-size:17px;margin:0;font-weight:700;letter-spacing:.2px;}
.brand-text p{margin:0;font-size:11.5px;color:var(--text-faint);font-family:var(--mono);letter-spacing:.4px;text-transform:uppercase;}

.topbar-actions{display:flex;gap:10px;align-items:center;}
.icon-btn{
  background:var(--panel);border:1px solid var(--border);color:var(--text-dim);
  width:36px;height:36px;border-radius:8px;display:flex;align-items:center;justify-content:center;
  cursor:pointer;transition:.15s;
}
.icon-btn:hover{color:var(--text);border-color:#3a4350;background:var(--panel-2);}
.icon-btn:active{transform:scale(.94);}
.icon-btn svg{width:16px;height:16px;}
.icon-btn.danger:hover{color:var(--loss);border-color:var(--loss);}

/* ===== Stats grid ===== */
.stats-grid{
  display:grid;grid-template-columns:repeat(8,1fr);gap:10px;margin-bottom:24px;
}
@media (max-width:1100px){.stats-grid{grid-template-columns:repeat(4,1fr);}}
@media (max-width:560px){.stats-grid{grid-template-columns:repeat(2,1fr);}}
.stat-card{
  background:var(--panel);border:1px solid var(--border-soft);border-radius:var(--radius);
  padding:13px 14px;display:flex;flex-direction:column;gap:6px;
  transition:border-color .15s;
}
.stat-card:hover{border-color:#2a323d;}
.stat-label{font-size:10.5px;color:var(--text-faint);text-transform:uppercase;letter-spacing:.5px;font-weight:600;}
.stat-value{font-family:var(--mono);font-size:18px;font-weight:600;letter-spacing:-.2px;}
.stat-value.win{color:var(--win);}
.stat-value.loss{color:var(--loss);}
.stat-value.accent{color:var(--accent);}

/* ===== Toolbar ===== */
.toolbar{
  display:flex;justify-content:space-between;align-items:center;margin-bottom:14px;gap:12px;flex-wrap:wrap;
}
.toolbar-left{display:flex;gap:10px;flex-wrap:wrap;align-items:center;}
.btn{
  font-family:var(--sans);font-size:13px;font-weight:600;
  border-radius:8px;padding:9px 16px;cursor:pointer;border:1px solid transparent;
  display:inline-flex;align-items:center;gap:7px;transition:.15s;white-space:nowrap;
}
.btn-primary{background:var(--accent);color:#0a0d12;}
.btn-primary:hover{background:#dcb872;}
.btn-ghost{background:var(--panel);border:1px solid var(--border);color:var(--text-dim);}
.btn-ghost:hover{color:var(--text);border-color:#3a4350;}
.btn:active{transform:scale(.97);}
.btn svg{width:14px;height:14px;}

select.filter-select, input.filter-input{
  background:var(--panel);border:1px solid var(--border);color:var(--text);
  font-family:var(--sans);font-size:13px;padding:8px 12px;border-radius:8px;outline:none;
}
select.filter-select:focus, input.filter-input:focus{border-color:var(--accent);}

/* ===== Table ===== */
.table-wrap{
  background:var(--panel);border:1px solid var(--border-soft);border-radius:var(--radius);
  overflow-x:auto;
}
table{width:100%;border-collapse:collapse;min-width:1180px;font-size:12.5px;}
thead th{
  position:sticky;top:0;background:var(--panel-2);
  text-align:left;padding:11px 12px;font-size:10.5px;color:var(--text-faint);
  text-transform:uppercase;letter-spacing:.5px;font-weight:600;border-bottom:1px solid var(--border);
  white-space:nowrap;
}
tbody td{
  padding:10px 12px;border-bottom:1px solid var(--border-soft);vertical-align:middle;
  font-family:var(--mono);white-space:nowrap;color:var(--text);
}
tbody tr{transition:background .12s;}
tbody tr:hover{background:rgba(255,255,255,0.018);}
tbody tr:last-child td{border-bottom:none;}

.pair-cell{font-family:var(--sans);font-weight:700;letter-spacing:.2px;}
.dir-tag, .result-tag, .session-tag{
  display:inline-block;padding:3px 9px;border-radius:5px;font-size:11px;font-weight:700;
  font-family:var(--sans);letter-spacing:.3px;
}
.dir-long{background:var(--win-soft);color:var(--win);}
.dir-short{background:var(--loss-soft);color:var(--loss);}
.result-win{background:var(--win-soft);color:var(--win);}
.result-loss{background:var(--loss-soft);color:var(--loss);}
.result-be{background:var(--be-soft);color:var(--be);}
.session-tag{background:rgba(202,164,94,0.1);color:var(--accent);}

.pnl-pos{color:var(--win);font-weight:600;}
.pnl-neg{color:var(--loss);font-weight:600;}

.systems-chip{
  font-family:var(--sans);font-size:11px;color:var(--text-dim);
  background:var(--panel-2);padding:3px 8px;border-radius:5px;border:1px solid var(--border-soft);
  white-space:normal;max-width:220px;display:inline-block;
}
.notes-cell{max-width:140px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-family:var(--sans);color:var(--text-dim);font-size:12px;}

.shot-thumbs{display:flex;gap:5px;}
.shot-thumb{
  width:32px;height:32px;border-radius:5px;object-fit:cover;cursor:pointer;
  border:1px solid var(--border);transition:.15s;
}
.shot-thumb:hover{border-color:var(--accent);transform:scale(1.08);}
.no-shot{color:var(--text-faint);font-size:11px;}

.row-actions{display:flex;gap:6px;}
.row-actions button{
  background:transparent;border:1px solid var(--border);color:var(--text-faint);
  width:28px;height:28px;border-radius:6px;cursor:pointer;display:flex;align-items:center;justify-content:center;
  transition:.15s;
}
.row-actions button:hover{color:var(--text);border-color:#3a4350;}
.row-actions button.del-btn:hover{color:var(--loss);border-color:var(--loss);}
.row-actions svg{width:13px;height:13px;}

.empty-state{
  text-align:center;padding:60px 20px;color:var(--text-faint);
}
.empty-state svg{width:42px;height:42px;margin-bottom:14px;opacity:.4;}
.empty-state p{font-size:13.5px;margin:0 0 4px;}
.empty-state span{font-size:12px;}

/* ===== Modal ===== */
.modal-overlay{
  position:fixed;inset:0;background:rgba(5,7,10,0.72);backdrop-filter:blur(3px);
  display:flex;align-items:flex-start;justify-content:center;z-index:1000;
  padding:30px 16px;overflow-y:auto;
}
.modal-overlay.hidden{display:none;}
.modal{
  background:var(--panel);border:1px solid var(--border);border-radius:14px;
  width:100%;max-width:640px;padding:24px;margin:auto 0;
}
.modal h2{margin:0 0 18px;font-size:17px;font-weight:700;}
.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:13px;}
@media (max-width:560px){.form-grid{grid-template-columns:1fr;}}
.form-grid .full{grid-column:1/-1;}
.field label{display:block;font-size:11px;color:var(--text-faint);text-transform:uppercase;letter-spacing:.4px;font-weight:600;margin-bottom:6px;}
.field input, .field select, .field textarea{
  width:100%;background:var(--bg);border:1px solid var(--border);color:var(--text);
  font-family:var(--sans);font-size:13.5px;padding:9px 11px;border-radius:8px;outline:none;transition:.15s;
}
.field input:focus, .field select:focus, .field textarea:focus{border-color:var(--accent);}
.field textarea{resize:vertical;min-height:64px;font-family:var(--sans);}

.dir-toggle{display:flex;gap:8px;}
.dir-toggle button{
  flex:1;padding:9px;border-radius:8px;border:1px solid var(--border);background:var(--bg);
  color:var(--text-dim);font-weight:700;font-size:13px;cursor:pointer;transition:.15s;
}
.dir-toggle button.active.long{background:var(--win-soft);color:var(--win);border-color:var(--win);}
.dir-toggle button.active.short{background:var(--loss-soft);color:var(--loss);border-color:var(--loss);}

.result-toggle{display:flex;gap:8px;}
.result-toggle button{
  flex:1;padding:9px;border-radius:8px;border:1px solid var(--border);background:var(--bg);
  color:var(--text-dim);font-weight:700;font-size:12.5px;cursor:pointer;transition:.15s;
}
.result-toggle button.active.win{background:var(--win-soft);color:var(--win);border-color:var(--win);}
.result-toggle button.active.loss{background:var(--loss-soft);color:var(--loss);border-color:var(--loss);}
.result-toggle button.active.be{background:var(--be-soft);color:var(--be);border-color:var(--be);}

.rr-display{
  font-family:var(--mono);font-size:13.5px;color:var(--accent);font-weight:600;
  padding:9px 11px;background:var(--bg);border:1px solid var(--border-soft);border-radius:8px;
}

/* Multiselect systems */
.multi-select{position:relative;}
.multi-select-display{
  background:var(--bg);border:1px solid var(--border);color:var(--text);
  font-family:var(--sans);font-size:13px;padding:9px 11px;border-radius:8px;cursor:pointer;
  min-height:38px;display:flex;align-items:center;flex-wrap:wrap;gap:5px;
}
.multi-select-display:empty::before, .multi-select-display .placeholder{color:var(--text-faint);}
.multi-select-dropdown{
  position:absolute;top:calc(100% + 4px);left:0;right:0;background:var(--panel-2);
  border:1px solid var(--border);border-radius:8px;padding:6px;z-index:50;
  max-height:230px;overflow-y:auto;box-shadow:0 10px 30px rgba(0,0,0,.4);
}
.multi-select-dropdown.hidden{display:none;}
.ms-option{
  display:flex;align-items:center;gap:9px;padding:8px 9px;border-radius:6px;cursor:pointer;font-size:13px;
}
.ms-option:hover{background:rgba(255,255,255,0.04);}
.ms-option input{accent-color:var(--accent);width:14px;height:14px;cursor:pointer;}
.ms-tag{
  background:var(--accent-soft);color:var(--accent);font-size:11px;font-weight:600;
  padding:2px 7px;border-radius:5px;
}

/* file upload */
.upload-zone{
  border:1.5px dashed var(--border);border-radius:8px;padding:16px;text-align:center;
  cursor:pointer;transition:.15s;background:var(--bg);
}
.upload-zone:hover{border-color:var(--accent);}
.upload-zone p{margin:0;font-size:12.5px;color:var(--text-faint);}
.upload-preview{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px;}
.upload-preview .thumb-wrap{position:relative;}
.upload-preview img{width:54px;height:54px;border-radius:6px;object-fit:cover;border:1px solid var(--border);}
.upload-preview .rm-thumb{
  position:absolute;top:-6px;right:-6px;width:18px;height:18px;border-radius:50%;
  background:var(--loss);color:#fff;border:none;font-size:11px;line-height:18px;cursor:pointer;
}

.modal-footer{display:flex;justify-content:flex-end;gap:10px;margin-top:20px;}

/* Lightbox */
.lightbox{
  position:fixed;inset:0;background:rgba(0,0,0,0.88);display:flex;align-items:center;justify-content:center;
  z-index:2000;padding:20px;
}
.lightbox.hidden{display:none;}
.lightbox img{max-width:92vw;max-height:88vh;border-radius:8px;}
.lightbox-close{
  position:absolute;top:20px;right:24px;width:38px;height:38px;border-radius:50%;
  background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.2);color:#fff;
  font-size:18px;cursor:pointer;display:flex;align-items:center;justify-content:center;
}
.lightbox-nav{
  position:absolute;top:50%;transform:translateY(-50%);width:42px;height:42px;border-radius:50%;
  background:rgba(255,255,255,0.08);border:1px solid rgba(255,255,255,0.2);color:#fff;cursor:pointer;
  display:flex;align-items:center;justify-content:center;font-size:18px;
}
.lightbox-nav.prev{left:20px;}
.lightbox-nav.next{right:20px;}

/* Toast */
#toast-wrap{position:fixed;bottom:20px;right:20px;display:flex;flex-direction:column;gap:8px;z-index:3000;}
.toast{
  background:var(--panel-2);border:1px solid var(--border);color:var(--text);
  padding:11px 16px;border-radius:8px;font-size:13px;box-shadow:0 8px 24px rgba(0,0,0,.4);
  animation:toastIn .2s ease;display:flex;align-items:center;gap:8px;
}
.toast.success{border-color:var(--win);}
.toast.danger{border-color:var(--loss);}
@keyframes toastIn{from{opacity:0;transform:translateY(8px);}to{opacity:1;transform:translateY(0);}}

.confirm-box .modal{max-width:380px;}
.confirm-box p{color:var(--text-dim);font-size:13.5px;line-height:1.5;margin:0 0 0;}

footer.app-footer{
  text-align:center;margin-top:34px;color:var(--text-faint);font-size:11.5px;font-family:var(--mono);
}
</style>
</head>
<body>

<div class="app">
  <header class="topbar">
    <div class="brand">
      <div class="brand-mark">TJ</div>
      <div class="brand-text">
        <h1>Trading Journal</h1>
        <p>Performance Log</p>
      </div>
    </div>
    <div class="topbar-actions">
      <button class="icon-btn" id="undoBtn" title="Undo (Ctrl+Z)">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 7v6h6"/><path d="M3 13a9 9 0 1 0 3-7.7L3 7"/></svg>
      </button>
      <button class="icon-btn" id="redoBtn" title="Redo (Ctrl+Y)">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M21 7v6h-6"/><path d="M21 13a9 9 0 1 1-3-7.7L21 7"/></svg>
      </button>
      <button class="icon-btn danger" id="clearAllBtn" title="Barchasini o'chirish">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18"/><path d="M8 6V4h8v2"/><path d="M19 6l-1 14H6L5 6"/></svg>
      </button>
    </div>
  </header>

  <section class="stats-grid" id="statsGrid"></section>

  <div class="toolbar">
    <div class="toolbar-left">
      <button class="btn btn-primary" id="addTradeBtn">
        <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4"><path d="M12 5v14M5 12h14"/></svg>
        Savdo qo'shish
      </button>
      <select class="filter-select" id="filterResult">
        <option value="">Barcha natijalar</option>
        <option value="Win">Win</option>
        <option value="Loss">Loss</option>
        <option value="BE">BE</option>
      </select>
      <select class="filter-select" id="filterSession">
        <option value="">Barcha sessiyalar</option>
        <option value="Asian">Asian</option>
        <option value="London">London</option>
        <option value="New York">New York</option>
      </select>
      <input type="text" class="filter-input" id="filterPair" placeholder="Juftlik bo'yicha qidirish...">
    </div>
  </div>

  <div class="table-wrap">
    <table>
      <thead>
        <tr>
          <th>Sana / Vaqt</th>
          <th>Juftlik</th>
          <th>Yo'nalish</th>
          <th>Entry</th>
          <th>SL</th>
          <th>TP</th>
          <th>RR</th>
          <th>Foyda/Zarar</th>
          <th>Natija</th>
          <th>Sessiya</th>
          <th>Sistema</th>
          <th>Screenshot</th>
          <th>Zametka</th>
          <th>Amallar</th>
        </tr>
      </thead>
      <tbody id="tableBody"></tbody>
    </table>
    <div class="empty-state" id="emptyState">
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6"><path d="M3 17l6-6 4 4 8-8"/></svg>
      <p>Hali savdolar yo'q</p>
      <span>Boshlash uchun "Savdo qo'shish" tugmasini bosing</span>
    </div>
  </div>

  <footer class="app-footer">Trading Journal &mdash; ma'lumotlar shu brauzerda lokal saqlanadi</footer>
</div>

<!-- Trade Modal -->
<div class="modal-overlay hidden" id="tradeModalOverlay">
  <div class="modal">
    <h2 id="modalTitle">Yangi savdo qo'shish</h2>
    <form id="tradeForm">
      <div class="form-grid">
        <div class="field">
          <label>Valyuta juftligi</label>
          <input type="text" id="f_pair" placeholder="EURUSD, XAUUSD..." required style="text-transform:uppercase;">
        </div>
        <div class="field">
          <label>Yo'nalish</label>
          <div class="dir-toggle">
            <button type="button" class="dir-btn long" data-val="Long">Long</button>
            <button type="button" class="dir-btn short" data-val="Short">Short</button>
          </div>
          <input type="hidden" id="f_direction" required>
        </div>
        <div class="field">
          <label>Entry narxi</label>
          <input type="number" step="any" id="f_entry" placeholder="0.00" required>
        </div>
        <div class="field">
          <label>Stop Loss</label>
          <input type="number" step="any" id="f_sl" placeholder="0.00">
        </div>
        <div class="field">
          <label>Take Profit</label>
          <input type="number" step="any" id="f_tp" placeholder="0.00">
        </div>
        <div class="field">
          <label>Risk / Reward</label>
          <div class="rr-display" id="f_rr_display">RR: —</div>
        </div>
        <div class="field">
          <label>Foyda / Zarar</label>
          <input type="text" id="f_pnl" placeholder="masalan: +120$ yoki -2.5%">
        </div>
        <div class="field">
          <label>Natija</label>
          <div class="result-toggle">
            <button type="button" class="res-btn win" data-val="Win">Win</button>
            <button type="button" class="res-btn loss" data-val="Loss">Loss</button>
            <button type="button" class="res-btn be" data-val="BE">BE</button>
          </div>
          <input type="hidden" id="f_result" required>
        </div>
        <div class="field">
          <label>Sessiya</label>
          <select id="f_session">
            <option value="Asian">Asian</option>
            <option value="London">London</option>
            <option value="New York">New York</option>
          </select>
        </div>
        <div class="field">
          <label>Sistema</label>
          <div class="multi-select" id="systemMultiSelect">
            <div class="multi-select-display" id="systemDisplay"><span class="placeholder">Sistemani tanlang...</span></div>
            <div class="multi-select-dropdown hidden" id="systemDropdown"></div>
          </div>
        </div>
        <div class="field full">
          <label>Zametka (Notes)</label>
          <textarea id="f_notes" placeholder="Savdo haqida izoh..."></textarea>
        </div>
        <div class="field full">
          <label>Screenshot(lar)</label>
          <div class="upload-zone" id="uploadZone">
            <p>Rasmlarni shu yerga tashlang yoki bosib tanlang</p>
            <input type="file" id="f_screenshots" accept="image/*" multiple style="display:none;">
          </div>
          <div class="upload-preview" id="uploadPreview"></div>
        </div>
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-ghost" id="cancelModalBtn">Bekor qilish</button>
        <button type="submit" class="btn btn-primary" id="saveTradeBtn">Saqlash</button>
      </div>
    </form>
  </div>
</div>

<!-- Confirm modal -->
<div class="modal-overlay hidden confirm-box" id="confirmOverlay">
  <div class="modal">
    <h2 id="confirmTitle">Tasdiqlash</h2>
    <p id="confirmText">Ushbu amalni bajarishni tasdiqlaysizmi?</p>
    <div class="modal-footer">
      <button type="button" class="btn btn-ghost" id="confirmCancelBtn">Yo'q</button>
      <button type="button" class="btn btn-primary" id="confirmOkBtn" style="background:var(--loss);color:#fff;">Ha, o'chirish</button>
    </div>
  </div>
</div>

<!-- Lightbox -->
<div class="lightbox hidden" id="lightbox">
  <button class="lightbox-close" id="lightboxClose">&times;</button>
  <button class="lightbox-nav prev" id="lightboxPrev">&#8249;</button>
  <img src="" id="lightboxImg">
  <button class="lightbox-nav next" id="lightboxNext">&#8250;</button>
</div>

<div id="toast-wrap"></div>

<script>
(function(){
"use strict";

/* ============ Constants ============ */
const STORAGE_KEY = "tj_trades_v1";
const SYSTEM_OPTIONS = ["Order Block","Order Flow","Breaker Block","FVG","IFVG","BPR","Iceberg","Klassika","CRT"];
const MAX_HISTORY = 50;

/* ============ State ============ */
let trades = [];          // array of trade objects
let history = [];         // undo stack of snapshots (JSON strings)
let future = [];          // redo stack
let editingId = null;
let selectedSystems = [];
let pendingShots = [];    // base64 strings for current modal session
let currentLightboxList = [];
let currentLightboxIndex = 0;
let confirmCallback = null;

/* ============ Utilities ============ */
function uid(){ return 't_' + Date.now().toString(36) + Math.random().toString(36).slice(2,8); }

function nowStamp(){
  const d = new Date();
  const pad = n => String(n).padStart(2,'0');
  return `${pad(d.getDate())}.${pad(d.getMonth()+1)}.${d.getFullYear()} ${pad(d.getHours())}:${pad(d.getMinutes())}`;
}

function formatRR(entry, sl, tp, dir){
  entry = parseFloat(entry); sl = parseFloat(sl); tp = parseFloat(tp);
  if(isNaN(entry) || isNaN(sl) || isNaN(tp)) return null;
  let risk, reward;
  if(dir === 'Long'){ risk = entry - sl; reward = tp - entry; }
  else { risk = sl - entry; reward = entry - tp; }
  if(risk <= 0 || reward <= 0) return null;
  return +(reward / risk).toFixed(2);
}

function escapeHtml(str){
  if(str == null) return '';
  return String(str).replace(/[&<>"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
}

function toast(msg, type){
  const wrap = document.getElementById('toast-wrap');
  const el = document.createElement('div');
  el.className = 'toast' + (type ? ' ' + type : '');
  el.textContent = msg;
  wrap.appendChild(el);
  setTimeout(()=> el.remove(), 2600);
}

/* ============ Persistence ============ */
function saveTrades(){
  try{
    localStorage.setItem(STORAGE_KEY, JSON.stringify(trades));
  }catch(e){
    toast("Saqlashda xatolik: hajm chegarasi (LocalStorage to'lgan bo'lishi mumkin)", "danger");
    console.error(e);
  }
}

function loadTrades(){
  try{
    const raw = localStorage.getItem(STORAGE_KEY);
    trades = raw ? JSON.parse(raw) : [];
  }catch(e){ trades = []; }
}

/* ============ History (Undo/Redo) ============ */
function snapshot(){
  return JSON.stringify(trades);
}

function pushHistory(){
  history.push(snapshot());
  if(history.length > MAX_HISTORY) history.shift();
  future = [];
}

function undo(){
  if(history.length === 0){ toast("Orqaga qaytarish uchun tarix yo'q"); return; }
  future.push(snapshot());
  const prev = history.pop();
  trades = JSON.parse(prev);
  saveTrades();
  renderAll();
  toast("Bekor qilindi (Undo)");
}

function redo(){
  if(future.length === 0){ toast("Qayta tiklash uchun tarix yo'q"); return; }
  history.push(snapshot());
  const next = future.pop();
  trades = JSON.parse(next);
  saveTrades();
  renderAll();
  toast("Qayta tiklandi (Redo)");
}

/* ============ Stats ============ */
function computeStats(list){
  const total = list.length;
  const wins = list.filter(t=>t.result === 'Win').length;
  const losses = list.filter(t=>t.result === 'Loss').length;
  const winRate = total > 0 ? (wins/total*100) : 0;

  let totalProfit = 0, totalLoss = 0, rrSum = 0, rrCount = 0;
  let maxWin = null, maxLoss = null;

  list.forEach(t=>{
    const val = parsePnlValue(t.pnl);
    if(val !== null){
      if(val > 0){ totalProfit += val; if(maxWin === null || val > maxWin) maxWin = val; }
      if(val < 0){ totalLoss += val; if(maxLoss === null || val < maxLoss) maxLoss = val; }
    }
    if(t.rr !== null && t.rr !== undefined && !isNaN(t.rr)){
      rrSum += parseFloat(t.rr); rrCount++;
    }
  });

  const net = totalProfit + totalLoss;
  const avgRR = rrCount > 0 ? (rrSum/rrCount) : 0;

  return {
    total, wins, losses, winRate, totalProfit, totalLoss, net, avgRR,
    maxWin: maxWin === null ? 0 : maxWin,
    maxLoss: maxLoss === null ? 0 : maxLoss
  };
}

// extracts numeric value from a pnl string like "+120$" "-2.5%" "120" etc.
function parsePnlValue(str){
  if(str == null || str === '') return null;
  const cleaned = String(str).replace(/[^0-9.\-+]/g,'');
  const num = parseFloat(cleaned);
  return isNaN(num) ? null : num;
}

function fmtMoney(n){
  const sign = n > 0 ? '+' : '';
  return sign + n.toFixed(2);
}

function renderStats(){
  const list = getFilteredTrades();
  const s = computeStats(list);
  const cards = [
    { label:'Jami savdolar', value: s.total, cls:'' },
    { label:'Win', value: s.wins, cls:'win' },
    { label:'Loss', value: s.losses, cls:'loss' },
    { label:'Win Rate', value: s.winRate.toFixed(1)+'%', cls:'accent' },
    { label:'Jami foyda', value: fmtMoney(s.totalProfit), cls:'win' },
    { label:'Jami zarar', value: fmtMoney(s.totalLoss), cls:'loss' },
    { label:'Sof natija', value: fmtMoney(s.net), cls: s.net >= 0 ? 'win' : 'loss' },
    { label:'O\'rtacha RR', value: s.avgRR.toFixed(2), cls:'accent' },
  ];
  document.getElementById('statsGrid').innerHTML = cards.map(c => `
    <div class="stat-card">
      <span class="stat-label">${c.label}</span>
      <span class="stat-value ${c.cls}">${c.value}</span>
    </div>
  `).join('') + `
    <div class="stat-card">
      <span class="stat-label">Eng katta Win</span>
      <span class="stat-value win">${fmtMoney(s.maxWin)}</span>
    </div>
    <div class="stat-card">
      <span class="stat-label">Eng katta Loss</span>
      <span class="stat-value loss">${fmtMoney(s.maxLoss)}</span>
    </div>
  `;
}

/* ============ Filtering ============ */
function getFilteredTrades(){
  const result = document.getElementById('filterResult').value;
  const session = document.getElementById('filterSession').value;
  const pairQuery = document.getElementById('filterPair').value.trim().toUpperCase();
  return trades.filter(t=>{
    if(result && t.result !== result) return false;
    if(session && t.session !== session) return false;
    if(pairQuery && !t.pair.toUpperCase().includes(pairQuery)) return false;
    return true;
  });
}

/* ============ Table Rendering ============ */
function renderTable(){
  const list = getFilteredTrades().slice().sort((a,b)=> b.createdAt - a.createdAt);
  const tbody = document.getElementById('tableBody');
  const emptyState = document.getElementById('emptyState');

  if(list.length === 0){
    tbody.innerHTML = '';
    emptyState.style.display = 'block';
    return;
  }
  emptyState.style.display = 'none';

  tbody.innerHTML = list.map(t => {
    const dirClass = t.direction === 'Long' ? 'dir-long' : 'dir-short';
    const resClass = t.result === 'Win' ? 'result-win' : (t.result === 'Loss' ? 'result-loss' : 'result-be');
    const pnlVal = parsePnlValue(t.pnl);
    const pnlClass = pnlVal === null ? '' : (pnlVal >= 0 ? 'pnl-pos' : 'pnl-neg');
    const systemsText = (t.systems && t.systems.length) ? t.systems.join(' + ') : '—';
    const shotsHtml = (t.screenshots && t.screenshots.length)
      ? `<div class="shot-thumbs">${t.screenshots.slice(0,3).map((s,i)=>`<img src="${s}" class="shot-thumb" data-tradeid="${t.id}" data-idx="${i}">`).join('')}${t.screenshots.length>3 ? `<span class="no-shot">+${t.screenshots.length-3}</span>`:''}</div>`
      : `<span class="no-shot">—</span>`;

    return `
    <tr data-id="${t.id}">
      <td>${escapeHtml(t.datetime)}</td>
      <td class="pair-cell">${escapeHtml(t.pair)}</td>
      <td><span class="dir-tag ${dirClass}">${t.direction}</span></td>
      <td>${t.entry ?? '—'}</td>
      <td>${t.sl ?? '—'}</td>
      <td>${t.tp ?? '—'}</td>
      <td>${t.rr !== null && t.rr !== undefined ? t.rr : '—'}</td>
      <td class="${pnlClass}">${escapeHtml(t.pnl) || '—'}</td>
      <td><span class="result-tag ${resClass}">${t.result}</span></td>
      <td><span class="session-tag">${escapeHtml(t.session)}</span></td>
      <td><span class="systems-chip">${escapeHtml(systemsText)}</span></td>
      <td>${shotsHtml}</td>
      <td class="notes-cell" title="${escapeHtml(t.notes || '')}">${escapeHtml(t.notes) || '—'}</td>
      <td>
        <div class="row-actions">
          <button class="edit-btn" data-id="${t.id}" title="Tahrirlash">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 20h9"/><path d="M16.5 3.5a2.1 2.1 0 0 1 3 3L7 19l-4 1 1-4Z"/></svg>
          </button>
          <button class="del-btn" data-id="${t.id}" title="O'chirish">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M3 6h18"/><path d="M8 6V4h8v2"/><path d="M19 6l-1 14H6L5 6"/></svg>
          </button>
        </div>
      </td>
    </tr>`;
  }).join('');
}

function renderAll(){
  renderStats();
  renderTable();
}

/* ============ Modal: Trade Form ============ */
const tradeModalOverlay = document.getElementById('tradeModalOverlay');
const tradeForm = document.getElementById('tradeForm');

function openTradeModal(trade){
  editingId = trade ? trade.id : null;
  document.getElementById('modalTitle').textContent = trade ? "Savdoni tahrirlash" : "Yangi savdo qo'shish";

  document.getElementById('f_pair').value = trade ? trade.pair : '';
  document.getElementById('f_entry').value = trade ? trade.entry : '';
  document.getElementById('f_sl').value = trade ? trade.sl : '';
  document.getElementById('f_tp').value = trade ? trade.tp : '';
  document.getElementById('f_pnl').value = trade ? trade.pnl : '';
  document.getElementById('f_notes').value = trade ? trade.notes : '';
  document.getElementById('f_session').value = trade ? trade.session : 'Asian';
  document.getElementById('f_direction').value = trade ? trade.direction : '';
  document.getElementById('f_result').value = trade ? trade.result : '';

  // direction buttons
  document.querySelectorAll('.dir-btn').forEach(b=>{
    b.classList.toggle('active', trade && trade.direction === b.dataset.val);
  });
  // result buttons
  document.querySelectorAll('.res-btn').forEach(b=>{
    b.classList.toggle('active', trade && trade.result === b.dataset.val);
  });

  selectedSystems = trade ? [...trade.systems] : [];
  renderSystemDisplay();

  pendingShots = trade ? [...(trade.screenshots || [])] : [];
  renderUploadPreview();

  updateRRDisplay();

  tradeModalOverlay.classList.remove('hidden');
  document.getElementById('f_pair').focus();
}

function closeTradeModal(){
  tradeModalOverlay.classList.add('hidden');
  tradeForm.reset();
  editingId = null;
  selectedSystems = [];
  pendingShots = [];
}

function updateRRDisplay(){
  const entry = document.getElementById('f_entry').value;
  const sl = document.getElementById('f_sl').value;
  const tp = document.getElementById('f_tp').value;
  const dir = document.getElementById('f_direction').value;
  const rr = formatRR(entry, sl, tp, dir);
  document.getElementById('f_rr_display').textContent = rr !== null ? `RR: 1 : ${rr}` : 'RR: —';
}

['f_entry','f_sl','f_tp'].forEach(id=>{
  document.getElementById(id).addEventListener('input', updateRRDisplay);
});

document.querySelectorAll('.dir-btn').forEach(btn=>{
  btn.addEventListener('click', ()=>{
    document.querySelectorAll('.dir-btn').forEach(b=>b.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById('f_direction').value = btn.dataset.val;
    updateRRDisplay();
  });
});

document.querySelectorAll('.res-btn').forEach(btn=>{
  btn.addEventListener('click', ()=>{
    document.querySelectorAll('.res-btn').forEach(b=>b.classList.remove('active'));
    btn.classList.add('active');
    document.getElementById('f_result').value = btn.dataset.val;
  });
});

/* ----- System multiselect ----- */
const systemDisplay = document.getElementById('systemDisplay');
const systemDropdown = document.getElementById('systemDropdown');

function renderSystemDropdownOptions(){
  systemDropdown.innerHTML = SYSTEM_OPTIONS.map(opt => `
    <label class="ms-option">
      <input type="checkbox" value="${opt}" ${selectedSystems.includes(opt) ? 'checked' : ''}>
      <span>${opt}</span>
    </label>
  `).join('');
}

function renderSystemDisplay(){
  if(selectedSystems.length === 0){
    systemDisplay.innerHTML = '<span class="placeholder">Sistemani tanlang...</span>';
  } else {
    systemDisplay.innerHTML = selectedSystems.map(s => `<span class="ms-tag">${escapeHtml(s)}</span>`).join('');
  }
  renderSystemDropdownOptions();
}

systemDisplay.addEventListener('click', (e)=>{
  e.stopPropagation();
  renderSystemDropdownOptions();
  systemDropdown.classList.toggle('hidden');
});

systemDropdown.addEventListener('click', (e)=>{
  e.stopPropagation();
  const checkbox = e.target.closest('input[type="checkbox"]');
  if(!checkbox) return;
  const val = checkbox.value;
  if(checkbox.checked){
    if(!selectedSystems.includes(val)) selectedSystems.push(val);
  } else {
    selectedSystems = selectedSystems.filter(s => s !== val);
  }
  renderSystemDisplay();
  systemDropdown.classList.remove('hidden');
  setTimeout(()=> systemDropdown.classList.remove('hidden'), 0);
});

document.addEventListener('click', (e)=>{
  if(!document.getElementById('systemMultiSelect').contains(e.target)){
    systemDropdown.classList.add('hidden');
  }
});

/* ----- Upload ----- */
const uploadZone = document.getElementById('uploadZone');
const fileInput = document.getElementById('f_screenshots');
const uploadPreview = document.getElementById('uploadPreview');

uploadZone.addEventListener('click', ()=> fileInput.click());
uploadZone.addEventListener('dragover', e=>{ e.preventDefault(); uploadZone.style.borderColor = 'var(--accent)'; });
uploadZone.addEventListener('dragleave', ()=> uploadZone.style.borderColor = 'var(--border)');
uploadZone.addEventListener('drop', e=>{
  e.preventDefault();
  uploadZone.style.borderColor = 'var(--border)';
  handleFiles(e.dataTransfer.files);
});
fileInput.addEventListener('change', e => handleFiles(e.target.files));

function handleFiles(fileList){
  Array.from(fileList).forEach(file=>{
    if(!file.type.startsWith('image/')) return;
    const reader = new FileReader();
    reader.onload = ev=>{
      pendingShots.push(ev.target.result);
      renderUploadPreview();
    };
    reader.readAsDataURL(file);
  });
  fileInput.value = '';
}

function renderUploadPreview(){
  uploadPreview.innerHTML = pendingShots.map((src, idx) => `
    <div class="thumb-wrap">
      <img src="${src}">
      <button type="button" class="rm-thumb" data-idx="${idx}">&times;</button>
    </div>
  `).join('');
}

uploadPreview.addEventListener('click', e=>{
  const btn = e.target.closest('.rm-thumb');
  if(!btn) return;
  const idx = parseInt(btn.dataset.idx);
  pendingShots.splice(idx,1);
  renderUploadPreview();
});

/* ----- Form submit ----- */
tradeForm.addEventListener('submit', e=>{
  e.preventDefault();

  const pair = document.getElementById('f_pair').value.trim().toUpperCase();
  const direction = document.getElementById('f_direction').value;
  const result = document.getElementById('f_result').value;

  if(!pair){ toast("Valyuta juftligini kiriting", "danger"); return; }
  if(!direction){ toast("Long yoki Short tanlang", "danger"); return; }
  if(!result){ toast("Natijani tanlang (Win/Loss/BE)", "danger"); return; }

  const entry = document.getElementById('f_entry').value;
  const sl = document.getElementById('f_sl').value;
  const tp = document.getElementById('f_tp').value;
  const rr = formatRR(entry, sl, tp, direction);

  const tradeData = {
    pair,
    direction,
    entry: entry !== '' ? parseFloat(entry) : null,
    sl: sl !== '' ? parseFloat(sl) : null,
    tp: tp !== '' ? parseFloat(tp) : null,
    rr,
    pnl: document.getElementById('f_pnl').value.trim(),
    result,
    session: document.getElementById('f_session').value,
    systems: [...selectedSystems],
    notes: document.getElementById('f_notes').value.trim(),
    screenshots: [...pendingShots],
  };

  pushHistory();

  if(editingId){
    const idx = trades.findIndex(t => t.id === editingId);
    if(idx !== -1){
      trades[idx] = { ...trades[idx], ...tradeData };
    }
    toast("Savdo yangilandi", "success");
  } else {
    trades.push({
      id: uid(),
      datetime: nowStamp(),
      createdAt: Date.now(),
      ...tradeData
    });
    toast("Savdo qo'shildi", "success");
  }

  saveTrades();
  renderAll();
  closeTradeModal();
});

document.getElementById('addTradeBtn').addEventListener('click', ()=> openTradeModal(null));
document.getElementById('cancelModalBtn').addEventListener('click', closeTradeModal);
tradeModalOverlay.addEventListener('click', e=>{ if(e.target === tradeModalOverlay) closeTradeModal(); });

/* ============ Edit / Delete ============ */
document.getElementById('tableBody').addEventListener('click', e=>{
  const editBtn = e.target.closest('.edit-btn');
  const delBtn = e.target.closest('.del-btn');
  const thumb = e.target.closest('.shot-thumb');

  if(editBtn){
    const trade = trades.find(t => t.id === editBtn.dataset.id);
    if(trade) openTradeModal(trade);
  } else if(delBtn){
    const id = delBtn.dataset.id;
    openConfirm("Savdoni o'chirish", "Ushbu savdo yozuvini o'chirishni tasdiqlaysizmi? Bu amalni Undo orqali qaytarish mumkin.", ()=>{
      pushHistory();
      trades = trades.filter(t => t.id !== id);
      saveTrades();
      renderAll();
      toast("Savdo o'chirildi", "danger");
    });
  } else if(thumb){
    const trade = trades.find(t => t.id === thumb.dataset.tradeid);
    if(trade){
      currentLightboxList = trade.screenshots;
      currentLightboxIndex = parseInt(thumb.dataset.idx);
      openLightbox();
    }
  }
});

/* ============ Confirm Modal ============ */
const confirmOverlay = document.getElementById('confirmOverlay');
function openConfirm(title, text, onConfirm){
  document.getElementById('confirmTitle').textContent = title;
  document.getElementById('confirmText').textContent = text;
  confirmCallback = onConfirm;
  confirmOverlay.classList.remove('hidden');
}
function closeConfirm(){
  confirmOverlay.classList.add('hidden');
  confirmCallback = null;
}
document.getElementById('confirmCancelBtn').addEventListener('click', closeConfirm);
document.getElementById('confirmOkBtn').addEventListener('click', ()=>{
  if(confirmCallback) confirmCallback();
  closeConfirm();
});
confirmOverlay.addEventListener('click', e=>{ if(e.target === confirmOverlay) closeConfirm(); });

document.getElementById('clearAllBtn').addEventListener('click', ()=>{
  if(trades.length === 0){ toast("Jurnal allaqachon bo'sh"); return; }
  openConfirm("Barchasini o'chirish", "Barcha savdo yozuvlari butunlay o'chiriladi. Tasdiqlaysizmi?", ()=>{
    pushHistory();
    trades = [];
    saveTrades();
    renderAll();
    toast("Barcha ma'lumotlar o'chirildi", "danger");
  });
});

/* ============ Lightbox ============ */
const lightbox = document.getElementById('lightbox');
function openLightbox(){
  document.getElementById('lightboxImg').src = currentLightboxList[currentLightboxIndex];
  lightbox.classList.remove('hidden');
}
function closeLightbox(){ lightbox.classList.add('hidden'); }
function lightboxNav(dir){
  if(currentLightboxList.length === 0) return;
  currentLightboxIndex = (currentLightboxIndex + dir + currentLightboxList.length) % currentLightboxList.length;
  document.getElementById('lightboxImg').src = currentLightboxList[currentLightboxIndex];
}
document.getElementById('lightboxClose').addEventListener('click', closeLightbox);
document.getElementById('lightboxPrev').addEventListener('click', ()=> lightboxNav(-1));
document.getElementById('lightboxNext').addEventListener('click', ()=> lightboxNav(1));
lightbox.addEventListener('click', e=>{ if(e.target === lightbox) closeLightbox(); });

/* ============ Filters ============ */
document.getElementById('filterResult').addEventListener('change', renderAll);
document.getElementById('filterSession').addEventListener('change', renderAll);
document.getElementById('filterPair').addEventListener('input', renderAll);

/* ============ Keyboard shortcuts ============ */
document.addEventListener('keydown', e=>{
  const ctrlOrCmd = e.ctrlKey || e.metaKey;
  if(ctrlOrCmd && e.key.toLowerCase() === 'z'){
    e.preventDefault();
    undo();
  } else if(ctrlOrCmd && e.key.toLowerCase() === 'y'){
    e.preventDefault();
    redo();
  } else if(e.key === 'Escape'){
    if(!lightbox.classList.contains('hidden')) closeLightbox();
    else if(!confirmOverlay.classList.contains('hidden')) closeConfirm();
    else if(!tradeModalOverlay.classList.contains('hidden')) closeTradeModal();
  }
});

document.getElementById('undoBtn').addEventListener('click', undo);
document.getElementById('redoBtn').addEventListener('click', redo);

/* ============ Init ============ */
loadTrades();
renderAll();

})();
</script>
</body>
</html>
